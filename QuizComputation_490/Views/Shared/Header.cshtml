
<nav class="navbar navbar-expand-lg bg-info px-5 py-3">
    @{
        if (@TempData.Peek("Role").Equals("Admin"))
        {
            <a class="navbar-brand" href="/Admin/GetAllQuiz">
                <i class="fa-solid fa-brain fa-2xl" style="color: white;"></i>
            </a>
        }
        else if (@TempData.Peek("Role").Equals("User"))
        {
            <a class="navbar-brand" href="/User/GetAllQuizes">
                <i class="fa-solid fa-brain fa-2xl" style="color: white;"></i>
            </a>
        }
    }
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse d-flex justify-content-between" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            @{
                if (@TempData.Peek("Role").Equals("Admin"))
                {
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/Admin/GetAllQuiz">All Quiz</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/Admin/CreateQuiz">Create Quiz</a>
                    </li>
                }
                else if (@TempData.Peek("Role").Equals("User"))
                {
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/User/GetAllQuizes">All Quiz</a>
                    </li>
                }
            }
        </ul>
        <div>
            <ul class="navbar-nav mr-auto">
                @{
                    if (@TempData.Peek("Role").Equals("Admin"))
                    {
                        <li>@Html.ActionLink("Profile", "ShowProfile", "Admin", null, new { @class = "nav-link text-white" })</li>
                    }
                    else if (@TempData.Peek("Role").Equals("User"))
                    {
                        <li>@Html.ActionLink("Profile", "ShowProfile", "User", null, new { @class = "nav-link text-white" })</li>
                    }
                }
                <li>@Html.ActionLink("Logout", "SignOut", "Login", null, new { @class = "nav-link text-white" })</li>
            </ul>
        </div>
    </div>
</nav>